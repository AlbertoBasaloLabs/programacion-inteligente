# Naming Conventions — {Product_Name}

## Domain Language

- **Business terms**: {Language} (e.g. `cohete`, `lanzamiento`, `reserva`)
- **Technical terms / suffixes**: {Language} (e.g. `Controller`, `Service`, `.component.ts`)
- **Code comments**: {Language}
- **Commit messages**: {Language or convention}

## Folders

### {tier}
- **Pattern**: {feature-based / layer-based / hybrid}
- **Casing**: {kebab-case / camelCase / snake_case}
- **Examples**: `{example1}`, `{example2}`

{Repeat for each tier.}

## Files

### {tier}
- **{role}**: `{pattern}` — `{example1}`, `{example2}`
- **{role}**: `{pattern}` — `{example1}`, `{example2}`

{Repeat for each tier.}

## Language Elements

### {Tier / Language}
- **Classes / Records / Components**: {PascalCase} — `{example}`
- **Interfaces / Types**: {PascalCase} — `{example}`
- **Methods / Functions**: {camelCase} — `{example}`
- **Variables / Fields**: {camelCase} — `{example}`
- **Constants**: {UPPER_SNAKE / camelCase} — `{example}`
- **Enums**: {PascalCase} — `{example}`
- **Enum members**: {PascalCase / UPPER_SNAKE} — `{example}`

{Repeat for each tier/language if they differ.}

## Detected Inconsistencies

- {Inconsistency description, with file examples. Or "No inconsistencies detected."}
