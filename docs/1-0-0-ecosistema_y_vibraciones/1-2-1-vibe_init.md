# Vibe init

Asegurar git init, repo clean, branch main.

## Node Express

1. Create at root folder a Node app with Typescript modern bare-bone setup.
2. Add a recent version Express to write an API with a health status endpoint. 
  
Cambiar de chat

3. Smoke test with Playwright and commit changes with conventional message.

## Visual Studio .NET

1. Create a new ASP.NET Core Web API project targeting .NET 10 with the name AstroBookings.
2. Add a a health status endpoint.
  
Cambiar de chat

3. Add Smoke tests with *.http files and commit changes with conventional message.