# Explore — greenfield mode

No source code exists yet. **Prescribe**: propose sensible defaults, ask only what you can't reasonably assume.

- **Explore**: confirm OS, shell, Git remote from the environment. With no code to read, gather product intent from the user — ask one closed question at a time for anything blocking (product goal, target stack, source layout).
- **AGENTS.md**: propose `{Product_Folder}` and `{Source_Folders}` (user can override). State the intended stack as a decision. Fill **Product** problem + solution from the user's brief; if absent, ask before writing.
- **Architecture**: prescribe the container split and tech per container. Diagrams describe the *intended* design; mark assumptions. Propose entities and relationships (no attributes/constraints).

## Guardrails
- Prefer one strong default over a menu of options.
- Never invent product requirements — ask.
